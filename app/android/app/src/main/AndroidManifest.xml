<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sem isto, NENHUMA chamada de rede funciona: nem para o mapa,
         nem para a API. O `flutter create` já inclui por padrão em alguns
         templates, mas é fácil sumir; deixamos explícito aqui. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Localização do usuário para o mapa (LocalizacaoProvider/geolocator).
         COARSE = aproximada (rede/wifi), FINE = precisa (GPS). Pedimos as
         duas: o Android decide qual usar conforme o hardware disponível. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- android:usesCleartextTraffic="true": ATENÇÃO, ISTO É SÓ PARA DESENVOLVIMENTO.
         Desde o Android 9 (API 28), o sistema BLOQUEIA por padrão qualquer
         tráfego HTTP puro (não criptografado), e nosso backend, rodando
         local com `npm run dev`, é http://10.0.2.2:3333, sem HTTPS. Sem esta
         linha, toda chamada à API falharia com "CleartextNotPermitted".
         Quando o backend for para produção com HTTPS de verdade (o que
         qualquer provedor sério oferece de graça, via Let's Encrypt), REMOVA
         esta linha: deixá-la em produção permitiria conexões inseguras. -->
    <application
        android:label="servicos_manaus_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
